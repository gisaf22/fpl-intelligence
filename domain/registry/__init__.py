"""Registry contract — schema, validation rules, and typed loader.

The registry is a research finding promoted by governance; its contract lives
in ``domain/`` (the shared leaf) so producer (research), validator/gate
(governance), and consumer (intelligence) all depend downward on one interface.
"""
